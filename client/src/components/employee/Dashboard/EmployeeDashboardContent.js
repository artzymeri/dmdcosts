import {observer} from "mobx-react-lite";

const EmployeeDashboardContent = () => {
    return (
        <div className="employee-ballina-content-container">
            <h2>
                Ballina
            </h2>
        </div>
    )
}

export default observer(EmployeeDashboardContent);